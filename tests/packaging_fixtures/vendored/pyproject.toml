[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "culvert-solver-vendored-smoke"
version = "1.0.0"
requires-python = ">=3.14,<3.15"

[tool.hatch.build.targets.wheel]
packages = ["src/culvert_solver"]
