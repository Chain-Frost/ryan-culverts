# API reference

The documented compatibility boundary is the names exported directly from
`culvert_solver`. Submodule paths remain implementation details unless another document
explicitly promotes them.

::: culvert_solver
    options:
      members: true
      filters:
        - "!^version$"
